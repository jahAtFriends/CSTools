package org.friendsbalt.javaIntro;

/**
* A {@code StrictGrid} is one in which items in the grid are aligned
* to the grid <i> strictly </i>. That is, an object can only have
* a location exactly in the center of each grid square and cannot
* move "through" the grid.
*/
public abstract class StrictGrid extends Grid {
	
	
	
}